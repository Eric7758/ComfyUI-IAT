## ComfyUI-IAT 最小额外依赖（不重复声明 ComfyUI 已内置的 torch/numpy/pillow）
## 说明：如你使用精简版 ComfyUI 环境，缺失基础科学计算依赖时请自行补装。

# Qwen3.5 文本/多模态模型推理框架
transformers>=4.57.0

# 插件配置读取（config.yaml）
PyYAML>=6.0

# 版本比较与兼容性判断（qwen35_runtime.py）
packaging>=24.0

# 模型自动下载源（主：ModelScope，备：HuggingFace Hub）
modelscope>=1.18.0
huggingface_hub>=0.24.0
